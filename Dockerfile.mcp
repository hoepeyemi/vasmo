FROM node:20-alpine AS base

RUN npm install -g pnpm@10

WORKDIR /repo
ENV CI=true

COPY package.json pnpm-lock.yaml pnpm-workspace.yaml ./
COPY app/package.json ./app/package.json
COPY agent/package.json ./agent/package.json
COPY contracts/package.json ./contracts/package.json

RUN pnpm install --frozen-lockfile --filter vasmo-agent...

COPY . .

WORKDIR /repo/agent

RUN pnpm build
RUN pnpm prune --prod

FROM node:20-alpine AS runner

WORKDIR /app/agent

ENV NODE_ENV=production
ENV WS_PORT=8080

COPY --from=base /repo/node_modules /app/node_modules
COPY --from=base /repo/agent/dist ./dist
COPY --from=base /repo/agent/package.json ./package.json
COPY --from=base /repo/contracts/deployments /app/contracts/deployments

EXPOSE 8080

HEALTHCHECK --interval=30s --timeout=5s --start-period=30s --retries=3 \
  CMD wget -qO- http://127.0.0.1:8080/health >/dev/null 2>&1 || exit 1

CMD ["node", "dist/index.js"]
